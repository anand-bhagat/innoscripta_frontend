version: '1'
services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - '3000:3000'
    environment:
      - REACT_APP_API_URL=http://127.0.0.1:8000/api
    volumes:
      - './frontend:/app'
